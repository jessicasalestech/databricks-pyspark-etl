# Copy to .env and fill in. Never commit real values.
# Used when running the notebooks outside Databricks for local smoke tests.

# Local Spark tuning
SPARK_MASTER=local[2]
SPARK_MEMORY=1g

# Databricks (optional, redacted placeholders only)
DATABRICKS_HOST=
DATABRICKS_TOKEN=
DATABRICKS_CLUSTER_ID=